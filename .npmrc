# Suppress warnings and info messages during install
# Set log level to error to hide deprecation warnings (like node-domexception)
# The node-domexception warning comes from webdriverio's dependency chain
# and doesn't affect functionality - Node.js has native DOMException support
loglevel=error
